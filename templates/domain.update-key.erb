### File managed with puppet ###
### Served by:        '<%= scope.lookupvar('::servername') %>'
### Module:           '<%= scope.to_hash['module_name'] %>'
### Template source:  '<%= template_source %>'

key <%=@server_domain%>. {
  algorithm HMAC-SHA512;
  secret "<%= @dhcp_update_key_secret %>";
};

